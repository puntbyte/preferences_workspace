name: console_example
description: >-
  A command-line application demonstrating the `preferences_annotation` and
  `preferences_generator` packages.

publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  preferences_annotation: ^3.0.0

dev_dependencies:
  build_runner: ^2.6.0
  preferences_generator: ^3.0.0
