// For RegularAppSettings
enum AppLanguage { english, spanish, french, german }
enum AppFontSize { small, medium, large }

// For SecureAppSettings
enum BiometricAuth { none, devicePin, fingerprint }
enum UserRole { guest, member, admin }