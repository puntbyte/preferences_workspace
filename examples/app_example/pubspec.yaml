name: app_example
description: A Flutter application demonstrating the preferences_generator package.

publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  flutter_secure_storage: ^9.2.4
  preferences_annotation: ^3.0.0
  shared_preferences: ^2.5.3

dev_dependencies:
  build_runner: ^2.6.0
  preferences_generator: ^3.0.0

flutter:
  uses-material-design: true
